import {StyleSheet} from 'react-native';

const MaskedCardNumberStyles = StyleSheet.create({
  maskedView: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  maskedGroup: {
    marginRight: 11.8,
  },
});

export default MaskedCardNumberStyles;
